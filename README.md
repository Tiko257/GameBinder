# GameBinder

Steam game card generator and printable library manager.

## Current checkpoint
V110.3.1 — Temporary Card Inherits Back Design.

The repository contains the project source code for the validated GameBinder checkpoint. Temporary App ID cards inherit the currently saved Back Design at creation time while subsequent edits remain temporary.
